--- internal/core/geo_init_test.go
+++ internal/core/geo_init_test.go
@@ -29,7 +29,7 @@

	_, err = writer.WriteTo(fh)
	require.NoError(t, err)
-	fh.Close()
+	require.NoError(t, fh.Close())

	// Save existing environment and set a custom one
	t.Setenv("MAXMIND_DB_PATH", dbPath)
