# --- SERVER SECRETS ---
# Generate a random 32-character string (e.g., openssl rand -hex 16)
SERVER_SECRET=dCo2fPTZt4FatzS8p7DhP4AxtZKMfReF

# --- INITIAL ADMIN USER ---
INITIAL_USER=admin
INITIAL_PASSWORD=securepassword123
INITIAL_EMAIL=admin@example.com
# Optional: Pre-set a 2FA secret for the admin
INITIAL_2FA_SECRET=Q7ND5X63PGNTKOMYLMM5K5OG77V55GSK

# --- REDIS CONFIGURATION ---
REDIS_HOST=rauth-auth-redis
REDIS_PORT=6379
REDIS_PASSWORD=YourStrongRedisPassword

# --- AUTHENTICATION SETTINGS ---
# Domain for the auth cookie (must start with a dot for subdomains)
COOKIE_DOMAIN=.yourdomain.com
# How long the session remains valid (10080 = 1 week)
AUTH_TOKEN_VALIDITY_MINUTES=10080
# Allowed hosts for redirection (security whitelist)
ALLOWED_HOSTS=auth.yourdomain.com,app1.yourdomain.com,app2.yourdomain.com,localhost

# --- GEO SERVICE CONFIGURATION ---
GEO_API_HOST=rauth-geo-service
GEO_API_PORT=3000

# MaxMind Credentials (needed for geo-service to download databases)
MAXMIND_ACCOUNT_ID=your_id
MAXMIND_LICENSE_KEY=your_key
GEOIP_EDITION_IDS=GeoLite2-Country
GEOIP_DATABASE_DIRECTORY=/srv/app/geoip
